% Taller de programación relacional: Ordenar elementos de una lista de acuerdo a una llave
% Harold Achicanoy

% Punto 1
declare
proc {Menor T1 T2 Minimo}
end

% Punto 2
declare
proc {PMinimo Lista Minimo}
end

fun {MinimoLista L}
   proc {$ M}
      {PMinimo L M}
   end
end
{Browse {SolveAll {MinimoLista [t(d 2) t(b 3) t(c 4) t(a 2) t(e 5)]}}}

% Punto 3
declare
proc {LOrd Lista ListaOrd}
end

fun {OrdenarLista Lista}
   proc {$ LO}
      {LOrd Lista LO}
   end
end
{Browse {SolveAll {OrdenarLista [t(d 2) t(b 3) t(c 4) t(a 2) t(e 5)]}}}